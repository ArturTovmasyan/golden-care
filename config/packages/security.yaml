security:
    encoders:
        App\Entity\User:
            algorithm: argon2i
    providers:
        main_provider:
            id: App\Provider\UserProvider

    firewalls:
        oauth_token:
            pattern:    ^/oauth/v2/token
            security:   false

        oauth_authorize:
            pattern:    ^/oauth/v2/auth
            anonymous: true
            security:  false
            stateless: true

        api_account:
            pattern:   ^/api/v1.0/account
            fos_oauth: false
            security:  false
            stateless: true
            anonymous: true

        api_user_complete_invitation:
            pattern:   ^/api/v1.0/dashboard/space/([0-9]+)/user/complete
            fos_oauth: false
            security:  false
            stateless: true
            anonymous: true

        api_report_csv_view:
            pattern:   ^/api/v1.0/report/csv-view/([a-z0-9]+)
            fos_oauth: false
            security:  false
            stateless: true
            anonymous: true

        api:
            pattern:   ^/api/v1.0/*
            fos_oauth: true
            stateless: true
            anonymous: true

        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            anonymous: true
    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        - { path: ^/api, roles: [ IS_AUTHENTICATED_FULLY ] }
