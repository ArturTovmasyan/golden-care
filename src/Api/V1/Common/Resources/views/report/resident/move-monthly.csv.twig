Move-in/Move-Out: {{ data.date|date("m/Y") }}

{{ data.strategy }},Resident, Move-in (Admit), Move-out (Discharge),Room/Bed #,Notes,"Total Time (Calculated based on 1 year = 365 days, 1 month = 30 days)"
{% for datum in data.data %}
    {{ datum.typeShorthand }},"{{ datum.firstName }} {{ datum.lastName }}",{% if datum.admissionType < constant('App\\Model\\AdmissionType::DISCHARGE') %}{{ datum.admitted|date("m/d/Y") }}{% endif %},{% if datum.admissionType == constant('App\\Model\\AdmissionType::DISCHARGE') %}{{ datum.admitted|date("m/d/Y") }}{% endif %},{% if datum.roomNumber is defined and datum.bedNumber is defined %}"{{ datum.roomNumber }} {{ datum.bedNumber }}"{% endif %},{% if datum.notes %}"{{ datum.notes }}"{% endif %},{% if data.days[datum['actionId']] is defined %}"{{ formatDay(data.days[datum['actionId']]) }}"{% endif %},
{% endfor %}
