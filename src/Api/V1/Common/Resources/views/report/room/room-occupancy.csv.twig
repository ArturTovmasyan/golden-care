{{ data.strategy }} Name, License Max, License Capacity, Available Rooms, Resident Count, Occupancy Rate
{% for datum in data.data %}
    {{ datum.name }},{{ datum.capacity }},{{ datum.licenseCapacity }},{{ datum.availableCount }},{{ datum.occupiedCount }},{% if datum.availableCount > 0 %}"{{ ((datum.occupiedCount / datum.availableCount) * 100)|number_format(2) }} %"{% else %}0.00 %{% endif %},
{% endfor %}
