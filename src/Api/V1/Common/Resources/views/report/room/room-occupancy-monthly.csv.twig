Occupancy - Cumulative: {{ data.dateStart|date("m/d/Y") }} through {{ data.dateEnd|date("m/d/Y") }}

{% for facility in data.data %}

    {{ data.strategy }},Historical Occupancy,{% for datum in facility %}{{ datum.month }},{% endfor %},
    {% for datum in facility %}{% if loop.first %}{{ datum.name }}{% endif %}{% endfor %},Potential Resident Days,{% for datum in facility %}{{ datum.potential }},{% endfor %},
    ,Actual Resident Days,{% for datum in facility %}{{ datum.actual }},{% endfor %},
    ,Occupancy(%),{% for datum in facility %}{{ datum.occupancy }},{% endfor %},

{% endfor %}
